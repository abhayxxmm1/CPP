# C-programming
git commit -m ""
git log 
git status
git add 
git add .   (for adding everything into the staging area i.e all the files in the current directory)
git checkout -- <fileName> (for rollBack)

Sudo mysql 
For pass word

HTTPS or SHH ( https://github.com/abhayxxmm1/command_line.git )


…or create a new repository on the command line

echo "# command_line" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/abhayxxmm1/command_line.git
git remote remove origin
git remote -v
git remote set-url origin git@github.com:abhayxxmm1/CPP.git
git branch
git push -u origin main

…or push an existing repository from the command line

git remote add origin git@github.com:abhayxxmm1/CPP.git
git branch -M main
git push -u origin main
git push -u origin main --force